__meteor_runtime_config__ = {};
//__meteor_runtime_config__.DDP_DEFAULT_CONNECTION_URL = 'http://localhost:3000';

// Development
// Open SystemPreferences/Network to find the computer's current IP address. It's probably not
// 10.0.0.4 but will look something like that. This IP allows you to 
// connect a mobile device to a locally run meteor server. Wifi must also be enabled on the 
// computer. 
// 
//__meteor_runtime_config__.DDP_DEFAULT_CONNECTION_URL = 'http://10.0.0.4:3000';

// Production
__meteor_runtime_config__.DDP_DEFAULT_CONNECTION_URL = 'https://beacon-server-a.herokuapp.com';
